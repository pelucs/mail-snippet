export interface Snippets {
  slug: string;
  title: string;
  category: string;
  reactCode: string;
  htmlCode: string;
}

export interface AdaptedSnippets {
  title: string;
  category: string;
  description: string;
  htmlCode: string;
}